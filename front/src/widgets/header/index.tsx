import { HeaderLogo, HeaderMenu } from "@/widgets";
const Header = () => {
  return (
    <header className="header">
      <HeaderLogo />
      <HeaderMenu />
    </header>
  );
};

export default Header;
