interface Member {
  memberId: number;
  phoneNumber: string;
  role: string;
  // password?: String;
  // nickname?: String;
  // phoneNumber?: String;
  // joinedAt?: Date;
  // withdrawalAt?: Date | null;
  // withdrawalYn?: Boolean;
}

export default Member;
